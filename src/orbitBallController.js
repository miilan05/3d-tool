// nisam jos poceo da pravim

class orbitBallController {
    constructor(mainCamera, ballCamera) {
      this.mainCamera = mainCamera
      this.ballCamera = ballCamera
    }
    
    updatePosition() {
        console.log("tek treba napravim")
    }
}

 export default orbitBallController 